version: 0.1

phases:
  build:
    commands:
      - npm install
      - npm run webpack
artifacts:
  files:
    - dist/bundle.js
    - images/*
    - src/*
    - styles/*
    - favicon.ico
    - index.html